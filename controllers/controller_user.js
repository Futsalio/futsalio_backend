const express = require('express')
const router = express.Router()
const db = require('../models')
const bodyParser = require('body-parser')

const _registerUser = function(req, res, next) {
    
}

module.exports = {

}